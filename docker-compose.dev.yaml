services:
  db:
    image: postgres:17
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=skinprice
      - POSTGRES_PASSWORD=skinprice
      - POSTGRES_DB=skinprice
